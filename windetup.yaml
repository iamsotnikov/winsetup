---
   - name: Create TEST Folder on Windows
     hosts: dc01
     gather_facts: false

     tasks:
     - name: Create TEST Folder
       win_file:
         path: "{{ test_folder_path }}\\{{ test_folder }}"
         state: directory
